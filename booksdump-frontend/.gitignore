# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
# Generated by `yarn build`, or by `make frontend-placeholder` when the real
# build is absent. The Go binary embeds this directory, so it must never be empty
# at compile time. The tracked source of the placeholder lives in placeholder/.
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
